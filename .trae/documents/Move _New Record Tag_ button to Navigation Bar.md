根据您的反馈，我已更新计划，包含调整导航栏按钮大小的步骤。

**更新后的计划**：

1. **移动按钮到导航栏并调整大小**：
   * 在 `TrainingRecordDetailView.swift` 的 `.toolbar` 中，将「新建动作标签」按钮 (`tag.badge.plus`) 添加到右侧。
   * 将新按钮与现有的「复制」按钮 (`doc.on.doc`) 放入同一个 `HStack` 中。
   * **缩小按钮图标**：为这两个按钮的图标应用 `.font(.system(size: 14))` (或类似的小号字体修饰符)，以减小它们的视觉尺寸，满足您“调小一点”的要求。

2. **移除原位置按钮**：
   * 删除页面底部原来的「新建动作标签」按钮代码。
   * 保留底部的「添加动作」按钮。

这样不仅完成了按钮位置的移动，也解决了导航栏图标过大的视觉问题。
